{
  "name": "escape-room",
  "version": "1.0.0",
  "main": "index.js",
  "author": "shreychowdhary <shrey.chowdhary1@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "husky": "^5.2.0"
  },
  "scripts": {
    "start": "(yarn build:client && cd server && yarn start:prod)",
    "start:dev": "(yarn start:client && cd server && yarn start:dev)",
    "start:client": "(cd client && yarn && yarn start)",
    "build:client": "(cd client && yarn && yarn build)"
  },
  "husky": {
    "hooks": {
      "pre-commit": "bash pre-commit.sh"
    }
  },
  "engines": {
    "node": "15.12.0"
  }
}